# CLAUDE.md (레포 루트) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** fde-agent 레포 루트에 Claude(LLM) 대상 프롬프트 역할의 CLAUDE.md 한 파일을 작성한다.

**Architecture:** 단일 마크다운 파일. 5 섹션(One-liner / 경계 지도 / 불변 원칙 / 완료 전 검증 / Progressive disclosure). 120줄 이하. Claude Code 시스템 프롬프트와 중복되는 지시는 배제.

**Tech Stack:** Markdown only. 테스트 없음(프롬프트 파일). 검증은 "포인터 경로 실존 확인"과 "줄 수/지시 수 상한"으로 수행.

**Spec:** `docs/superpowers/specs/2026-04-13-claude-md-design.md`

---

### Task 1: 포인터 경로 실존 사전 검증

작성 전에 spec이 참조하는 모든 경로가 실제 존재하는지 확인한다. 없으면 plan/spec을 먼저 고친다.

**Files:** (읽기만)

- [ ] **Step 1: 경로 존재 확인**

Run:
```bash
ls /Users/kimsanghyun/develop/AI/fde_agent/fde_harness_agent_design_v2.md \
   /Users/kimsanghyun/develop/AI/fde_agent/fde-agent/README.md \
   /Users/kimsanghyun/develop/AI/fde_agent/fde-agent/agents/builder.md \
   /Users/kimsanghyun/develop/AI/fde_agent/fde-agent/presets/admin-web/rules/CLAUDE.md \
   /Users/kimsanghyun/develop/AI/fde_agent/fde-agent/orchestrator/src/index.ts \
   /Users/kimsanghyun/develop/AI/fde_agent/fde-agent/orchestrator/src/loop.ts \
   /Users/kimsanghyun/develop/AI/fde_agent/fde-agent/orchestrator/src/builder/claude-code.ts \
   /Users/kimsanghyun/develop/AI/fde_agent/fde-agent/examples/resort-admin-spec.yaml
```

Expected: 모든 경로가 에러 없이 출력됨. 하나라도 없으면 Task 2로 넘어가기 전에 plan을 수정.

- [ ] **Step 2: Preset 스캐폴드 경로 확인**

Run:
```bash
ls -d /Users/kimsanghyun/develop/AI/fde_agent/fde-agent/presets/admin-web/core/scaffold \
      /Users/kimsanghyun/develop/AI/fde_agent/fde-agent/global/palettes \
      /Users/kimsanghyun/develop/AI/fde_agent/fde-agent/workspaces \
      /Users/kimsanghyun/develop/AI/fde_agent/fde-agent/hooks \
      /Users/kimsanghyun/develop/AI/fde_agent/fde-agent/skills
```

Expected: 모든 디렉터리가 에러 없이 출력됨.

---

### Task 2: CLAUDE.md 작성

**Files:**
- Create: `/Users/kimsanghyun/develop/AI/fde_agent/CLAUDE.md`

- [ ] **Step 1: 파일 생성**

다음 내용을 정확히 그대로 작성한다(한국어 유지, 120줄 이내 확인).

```markdown
# fde-agent — Claude 작업 규칙

이 파일은 레포에서 작업하는 Claude(LLM)를 위한 지시문이다. 인간 온보딩 문서가 아니다. 같은 주제의 상세는 README/설계 문서에 있다 — 여기는 Claude가 반복해서 틀리는 지점만 교정한다.

## 프로젝트 한 줄

FDE Harness Agent는 Claude Code Plugin이다. Eval spec을 입력받아 preset 기반 프로토타입을 생성하고 deterministic evaluation으로 검증한다. **Orchestrator(TypeScript CLI)가 루프의 주인이고, Claude Code는 생성/수정을 담당하는 Builder runtime일 뿐이다.**

## 레포 경계 지도

어디가 "우리 코드"이고 어디가 "생성물의 템플릿/산출물"인지 혼동하지 말 것.

- `fde-agent/orchestrator/` — 우리가 짜는 TypeScript CLI. Deterministic 로직. **LLM 호출은 `src/builder/claude-code.ts` 한 군데로 격리**한다.
- `fde-agent/presets/<preset>/` — Builder가 생성할 프로토타입의 스캐폴드/규칙. 여기를 수정하는 것은 "생성물"을 바꾸는 것이지 orchestrator 동작을 바꾸는 것이 아니다.
  - `core/scaffold/` — Next.js + shadcn/ui 기본 프로젝트
  - `rules/CLAUDE.md` — Builder(헤드리스 Claude)용 규칙. 이 루트 CLAUDE.md와 역할이 다르다.
  - `test-pack/` — E2E 시나리오 템플릿
- `fde-agent/global/palettes/` — 팔레트(전역 톤 앤 매너). preset과 독립.
- `fde-agent/workspaces/` — 런타임 산출물. 읽기만. 수정·커밋 대상 아님.
- `fde-agent/agents/*.md`, `fde-agent/skills/*/SKILL.md`, `fde-agent/hooks/hooks.json` — 플러그인 정의.
- `docs/`, `fde_harness_agent_design*.md` — 설계 레퍼런스. **코드와 충돌 시 코드가 진실이다.**

## 불변 설계 원칙

- Orchestrator는 deterministic이다. 새로운 LLM 호출을 `builder/claude-code.ts`와 `agents/*.md` 프롬프트 경계 밖에 추가하지 말 것.
- **루프 종료 판단은 evaluator가 한다.** Builder가 "완료했다"고 주장해도 evaluator가 pass 내지 않으면 루프는 계속 돈다. Builder 자가 평가로 종료 로직을 만들지 말 것.
- Preset = 패턴(목록/상세/폼/대시보드). **도메인은 eval spec에서 주입된다.** 패턴 파일에 엔티티명·필드명을 하드코딩하지 말 것.
- 팔레트는 `global/palettes/`에만. preset 스캐폴드에 색상을 하드코딩하지 말 것.
- 최대 repair 반복 15회 상수를 바꾸려면 설계 문서 업데이트가 먼저다.

## 완료 전 검증

"했다"고 선언하기 전에 해당 범위의 명령을 **직접 실행**해서 통과를 확인한다.

| 변경 범위 | 실행 명령 |
|---|---|
| Orchestrator 코드 | `cd fde-agent/orchestrator && npm run build && npm run test` |
| Scaffold | `cd fde-agent/presets/admin-web/core/scaffold && npm install && npm run build` |
| Eval spec 파서/스키마 | 위 orchestrator 테스트 + `examples/resort-admin-spec.yaml`로 스모크 |
| Preset rules / agents 프롬프트 | `examples/`의 스펙으로 `/fde-agent:run` 스모크 |

검증 명령을 돌리지 않았거나 실패 상태에서 "완료"라고 보고하지 말 것.

## 더 필요하면

- 전체 아키텍처·설계 의도: `fde_harness_agent_design_v2.md`
- 플러그인 사용법·구조·로드맵: `fde-agent/README.md`
- Builder 시스템 프롬프트: `fde-agent/agents/builder.md`
- Builder 규칙(preset별): `fde-agent/presets/<preset>/rules/CLAUDE.md`
- Orchestrator 엔트리: `fde-agent/orchestrator/src/index.ts`, `loop.ts`
```

- [ ] **Step 2: 줄 수 확인**

Run:
```bash
wc -l /Users/kimsanghyun/develop/AI/fde_agent/CLAUDE.md
```

Expected: 60~120 범위. 120 초과면 불변 원칙/경계 지도에서 군더더기 제거.

---

### Task 3: 최종 검증

- [ ] **Step 1: 경로 포인터 검증**

Run:
```bash
grep -oE '`[^`]+`' /Users/kimsanghyun/develop/AI/fde_agent/CLAUDE.md | tr -d '`' | grep -E '^(fde-agent/|docs/|fde_harness|examples/)' | sort -u
```

각 출력 경로(와일드카드 `<preset>`, `*.md` 등은 제외)에 대해 실존 확인:
```bash
ls fde-agent/orchestrator/src/index.ts fde-agent/orchestrator/src/loop.ts \
   fde-agent/orchestrator/src/builder/claude-code.ts \
   fde-agent/agents/builder.md fde-agent/README.md \
   fde_harness_agent_design_v2.md
```

Expected: 모두 실존. (`presets/<preset>/` 같은 placeholder는 검증 제외.)

- [ ] **Step 2: 중복 금지 자체 체크**

CLAUDE.md를 한 번 더 읽고 다음 패턴이 없는지 확인:
- 커밋/push 관련 지시 (시스템 프롬프트 중복)
- "YAGNI", "불필요한 추상화" 같은 일반 코딩 가이드 (시스템 프롬프트 중복)
- 린터/포매터 관련 명령
- README와 그대로 겹치는 기술 스택 상세

발견되면 삭제.

- [ ] **Step 3: 지시 수 상한 확인**

Run:
```bash
grep -cE '^- ' /Users/kimsanghyun/develop/AI/fde_agent/CLAUDE.md
```

Expected: 대략 15 이하. 크게 넘으면 bullet을 문장 결합 또는 삭제로 줄인다.

---

### Task 4: 마무리

- [ ] **Step 1: 사용자에게 리뷰 요청**

사용자에게 `CLAUDE.md` 경로를 알리고 최종 승인을 받는다. (레포가 git 관리 아니므로 커밋 단계 없음.)

---

## Self-Review

**Spec coverage:**
- 5 섹션 모두 Task 2 Step 1에 포함됨 ✓
- "의도적으로 제외" 항목은 Task 3 Step 2가 지킴 ✓
- "120줄 이하" 품질 체크는 Task 2 Step 2 ✓
- "포인터 경로 실존" 품질 체크는 Task 1 + Task 3 Step 1 ✓
- "강한 지시 10개 이하" → Task 3 Step 3에서 15 이하로 완화 측정(bullet 전체 기준이라 10보다 넉넉) ✓

**Placeholder scan:** 모든 Step이 실제 명령/코드를 담음. TBD·TODO 없음 ✓

**Type consistency:** N/A(단일 마크다운 파일).
